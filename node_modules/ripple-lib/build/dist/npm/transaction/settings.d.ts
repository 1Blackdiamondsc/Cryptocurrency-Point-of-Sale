import { Instructions, Prepare } from './types';
import { Memo } from '../common/types/objects';
export declare type WeightedSigner = {
    address: string;
    weight: number;
};
export declare type SettingsSigners = {
    threshold?: number;
    weights: WeightedSigner[];
};
export declare type Settings = {
    passwordSpent?: boolean;
    requireDestinationTag?: boolean;
    requireAuthorization?: boolean;
    disallowIncomingXRP?: boolean;
    disableMasterKey?: boolean;
    enableTransactionIDTracking?: boolean;
    noFreeze?: boolean;
    globalFreeze?: boolean;
    defaultRipple?: boolean;
    emailHash?: string;
    messageKey?: string;
    domain?: string;
    transferRate?: number;
    regularKey?: string;
    signers?: SettingsSigners;
    memos?: Memo[];
};
declare function prepareSettings(address: string, settings: Settings, instructions?: Instructions): Promise<Prepare>;
export default prepareSettings;
