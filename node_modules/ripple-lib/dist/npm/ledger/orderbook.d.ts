import { OrderSpecification } from './types';
import { Amount, Issue } from '../common/types/objects';
import { RippleAPI } from '../api';
export declare type OrdersOptions = {
    limit?: number;
    ledgerVersion?: number;
};
export declare type Orderbook = {
    base: Issue;
    counter: Issue;
};
export declare type OrderbookItem = {
    specification: OrderSpecification;
    properties: {
        maker: string;
        sequence: number;
        makerExchangeRate: string;
    };
    state?: {
        fundedAmount: Amount;
        priceOfFundedAmount: Amount;
    };
};
export declare type OrderbookOrders = Array<OrderbookItem>;
export declare type GetOrderbook = {
    bids: OrderbookOrders;
    asks: OrderbookOrders;
};
export default function getOrderbook(this: RippleAPI, address: string, orderbook: Orderbook, options?: OrdersOptions): Promise<GetOrderbook>;
